/*package dy.whatsong.streaming;

import dy.whatsong.domain.streaming.entity.room.Status;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class StreamingAPITest {

    @Test
    public void streamStatusPauseOrNot(){
        //given
        Status compareStatus = Status.PLAYING;
        //when
        Boolean ifPause = Status.statusPause(compareStatus);
        //then
        Assertions.assertThat(ifPause).isEqualTo(false);
    }
}
*/
